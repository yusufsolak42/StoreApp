namespace Entities.Dtos
{
    public record ProductDtoForUpdate : ProductDto //only records can inherit from record. Record is like class, but immutable. once created, u cant change.
    {
        
    }
}